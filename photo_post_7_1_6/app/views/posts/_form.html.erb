<%= form_with(model: post, local: true) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul><% post.errors.full_messages.each { |m| concat content_tag(:li, m) } %></ul>
    </div>
  <% end %>

  <p><%= f.label :title, "タイトル" %><br><%= f.text_field :title %></p>
  <p><%= f.label :content, "投稿内容" %><br><%= f.text_area :content %></p>
  <p><%= f.label :image, "画像" %><br><%= f.file_field :image, direct_upload: true %></p>

  <%= f.submit %>
<% end %>
